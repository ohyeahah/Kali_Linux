# Kali_Linux

* Day 1 - 2025.10.18
  * check cpu(list cpu)
    lscpu : list cpu
  * check memory, check list block, check ip address
    free -h : check memory
    lsblk : check list block
    ip a: check IP address
  * test network connectivity in Linux
    ping: send a signal
    ping -c (number): ping count number, it repeat as number
  * test network connectivity in Window
    ping -n (number): it repeat as number

